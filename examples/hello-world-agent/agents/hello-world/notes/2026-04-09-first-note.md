# First Note

This is the hello-world agent's first private note. It exists so the
signal aggregator has something to emit when it walks
`agents/hello-world/notes/` — not because the hello-world agent has
anything interesting to remember.

The content-trust separation contract marks this paragraph as
`semi-trusted` content even though the surrounding metadata (path,
mtime, id) is `trusted`.
