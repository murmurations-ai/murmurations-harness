/**
 * Tool boundary — Proposal 07 Phase 0 re-exports.
 *
 * Phase 3 will add `ToolRegistry`, `ToolInvocationRecorder`,
 * policy checks, and the `budget_remaining()` built-in here.
 * Phase 0 ships the descriptor and receipt types only.
 */

export * from "./registry.js";
export * from "./receipts.js";
