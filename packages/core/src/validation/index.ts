/**
 * Validation and health boundary — Proposal 07 Phase 0 re-exports.
 *
 * Phase 5 will add `WakeHealthMetrics` derivation, `HealthState` policy
 * evaluation, and the `LangfuseMetricsSource` here.
 */

export * from "./health.js";
