# @murmurations-ai/dashboard-tui

Terminal dashboard for the Murmuration Harness. Connects to a running daemon via Unix socket and displays agent state, governance rounds, and cost tracking in a terminal UI.

## Usage

```bash
murmuration-dashboard --root /path/to/murmuration
```

Or use the main CLI's attach mode for an interactive REPL:

```bash
murmuration attach my-murmuration
```

## Part of the Murmuration Harness

This package is part of the [Murmuration Harness](https://github.com/murmurations-ai/murmurations-harness) monorepo. See the root README for the full project overview.
